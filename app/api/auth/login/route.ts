import {NextResponse} from "next/server";
import {authenticate,createSession,sessionCookie} from "@/lib/authStore";
import {migrateWorkspace} from "@/lib/connectorStore";
import {cookies} from "next/headers";
export async function POST(req:Request){try{const body=await req.json().catch(()=>({}));const email=String(body.email||"").trim().toLowerCase(),password=String(body.password||"");const user=await authenticate(email,password);if(!user)return NextResponse.json({ok:false,error:"Incorrect email or password."},{status:401});const c=await cookies();const oldWorkspace=c.get("tentran_workspace")?.value;if(oldWorkspace&&oldWorkspace!==user.id)await migrateWorkspace(oldWorkspace,`user:${user.id}`);const sid=await createSession(user.id);const r=NextResponse.json({ok:true,user:{id:user.id,name:user.name,email:user.email}});r.cookies.set(sessionCookie(sid));r.cookies.set({name:"tentran_workspace",value:user.id,httpOnly:true,sameSite:"lax",secure:process.env.NODE_ENV==="production",path:"/",maxAge:60*60*24*365});return r;}catch(error){return NextResponse.json({ok:false,error:error instanceof Error?error.message:"Unable to sign in."},{status:400});}}
