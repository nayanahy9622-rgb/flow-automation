import {NextResponse} from "next/server";import {workspace,listApprovals,approveExecution} from "@/lib/runtimeStore";
export async function GET(){const w=await workspace().catch(()=>null);if(!w)return NextResponse.json({error:"unauthorized"},{status:401});return NextResponse.json({data:await listApprovals(w)});}
export async function POST(req:Request){const w=await workspace().catch(()=>null);if(!w)return NextResponse.json({error:"unauthorized"},{status:401});const b=await req.json().catch(()=>({}));if(typeof b.executionId!=="string"||typeof b.approved!=="boolean")return NextResponse.json({error:"executionId_and_approved_required"},{status:400});try{return NextResponse.json({ok:true,data:await approveExecution(w,b.executionId,b.approved)});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"approval_failed"},{status:409});}}
